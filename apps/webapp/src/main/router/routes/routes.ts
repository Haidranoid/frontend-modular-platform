import { RouteObject } from 'react-router'
import { authThunks } from '@webapp/auth'
//import { routes as globalRoutes } from '@webapp/global'
//import { routes as usersRoutes } from '@webapp/users'
//import * as auth from '@webapp/auth'

console.log({authThunks})

export const routes: RouteObject[] = [{}]
//export const routes: RouteObject[] = [...authRoutes, ...globalRoutes, ...usersRoutes]
