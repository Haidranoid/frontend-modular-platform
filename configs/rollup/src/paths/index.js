const { rollupBinPath } = require('./rollup-bin-path')
const { rollupConfigPath, rollupConfigTypesPath } = require('./rollup-config-paths')

module.exports = {
  rollupBinPath,
  rollupConfigPath,
  rollupConfigTypesPath,
}
