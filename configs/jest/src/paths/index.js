const { jestBinPath } = require('./jest-bin-path')
const { jestConfigPath } = require('./jest-config-path')
const { babelConfigPath } = require('./babel-config-path')

module.exports = {
    jestBinPath,
    jestConfigPath,
    babelConfigPath,
}