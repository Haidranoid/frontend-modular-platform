const { jestConfig } = require('./configs')
const { jestBinPath, jestConfigPath, babelConfigPath } = require('./paths')

module.exports = {
    jestConfig,
    jestBinPath,
    jestConfigPath,
    babelConfigPath
}